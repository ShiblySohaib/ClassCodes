1. SELECT customers.name FROM customers INNER JOIN orders ON customers.id = orders.customer_id HAVING COUNT(*)>2;
2. SELECT customers.name, COUNT(*) FROM customers INNER JOIN orders ON customers.id = orders.customer_id GROUP BY orders.customer_id ORDER BY COUNT(*) DESC;
3. 
